# Session 4 — Implement only approved work

Read START_HERE, MASTER_CONTEXT and WORKFLOW first, along with the saved checkpoint and relevant live records. This prompt does not authorize itself: the dispatcher must have admitted this stage. Use actual available tools and current founder scope. Checkpoint evidence at each stage boundary. Then follow the saved execution policy and reference/AUTONOMOUS_CONTINUATION.md: continue already-authorized work in the coordinating task, or stop for the specific unmet gate/blocker. A routine boundary does not require another founder message in autonomous mode.

## Entry and authority
Be the single code integrator. Require G2/G3 approvals or a specifically authorized repair route from QA/release. Read the exact named batches, scope, acceptance tests and exclusions. On a repair loop implement only the repair batch; do not redo discovery/design or reopen all improvements. No application edits without applicable approval.

Verify current SHA/dirty work and preserve user changes. Prefer small local commits only when authorized; stage explicit files/hunks, never broad unknown work. Do not fetch/reset/clean/stash/rebase to simplify integration. Isolated worktrees are optional tools, not a founder chore. The coordinator handles redacted context and reconciling changes; never assume ignored workflow state or dirty app files appear in a worktree automatically.

## Implement in dependency order
First safe execution/environment controls, then demonstrated cost/security/identity/backend/subscription defects and integration correctness, then approved shared design/motion and screens. Reuse existing capabilities and avoid speculative rewrites or unrelated upgrades. Where feasible add a failing reproduction/regression before fixing.

For each coherent batch: verify the problem → implement minimal adequate change → run targeted checks → review diff → get independent review on risky changes → execute actual native/service behavior under the approved envelope → record evidence. Expand critical regression coverage as integrations become available. Do not hide failed tests, suppress safety checks, mark mocks as native evidence or ignore dangling work solely to make a command green.

Rehearse migrations on isolated data. Before any approved production mutation, present exact project/operation, data impact, cost, candidate, backup/recovery and verification. No database reset. Credential rotation, settings changes, resource purchase and pricing updates need their own appropriate gate; an implementation plan alone does not authorize them.

Verify the chosen motion consistently across the approved interactions and accessibility modes. Validate loading/retry behavior, not only an ideal screen recording. Ensure all production-bound code removes/disables development mocks/prototype routes and unapproved variant overrides without destroying useful local comparison work.

## Scope and completion
New findings that fit the named acceptance scope may be repaired within it; changed product/commercial/privacy/production scope requires a new decision. Record findings and recommendation first for unapproved work. Keep real evidence on applied migrations, config and integrations; “implemented” differs from “verified in target environment.”

Update WORK and QA per batch, STATE after changed architecture/config, DECISIONS only for real decisions, and candidate identity before handoff. Session 4 exits with a named candidate, reviewed changes, targeted/critical regression evidence and known limitations; it does not call the app launch-ready or perform its own independent QA acceptance.

Set next_stage=5 and validate the checkpoint. In approved autonomous mode the coordinator immediately dispatches a fresh independent QA task with no implementation role and a minimal candidate/authority/evidence brief. QA returns evidence; only the coordinator updates checkpoints. No uploads or shipping here.
