# Session 3 — Native design and motion choices

Read START_HERE, MASTER_CONTEXT and WORKFLOW first, along with the saved checkpoint and relevant live records. This prompt does not authorize itself: the dispatcher must have admitted this stage. Use actual available tools and current founder scope. Checkpoint evidence at each stage boundary. Then follow the saved execution policy and reference/AUTONOMOUS_CONTINUATION.md: continue already-authorized work in the coordinating task, or stop for the specific unmet gate/blocker. A routine boundary does not require another founder message in autonomous mode.

## Entry and authority
Require discovery/audit decisions, a safe prototype environment and approved G2 prototype envelope. Work in an isolated development prototype. Only an explicitly approved smallest baseline-enablement batch may precede demos. No production configuration or whole-app rollout; no new visual direction by assumption.

## Observe the actual app
Record current Settings opening/closing from the feed, navigation/back gestures, tab/feed transitions, favoriting, secondary screens and loading states. Diagnose rendering stalls/layout changes before masking them with transitions. Record device/runtime/build and real performance evidence separately from screen recordings. Read existing tokens and motion/navigation implementation; preserve useful architecture.

## Demonstrate three comparable choices
Preserve the current baseline. Build these in the actual Expo/React Native native app, on the same content/layout:
A — native-first presentation appropriate to the Settings screen;
B — restrained translation/fade with minimal visual disturbance;
C — controlled spring motion without excessive bounce.

Show side-by-side or sequential normal-speed recordings plus an interactive way to compare. Include close/back, cancellation, rapid taps, interruption, gesture completion and Reduce Motion. A generated video, a web imitation or a written animation description is not acceptance evidence. An Expo Go limitation must not be presented as a successful custom-native test.

Ask the founder to choose a demo or specify a precise hybrid. Do not assume silence approves your favorite. After selection, show its application to representative pressed feedback, favorites, feed/tab and secondary navigation. Explain why different interaction purposes may need different parameters rather than one animation everywhere.

## Broader UI/UX refinement
Show proposed improvements to reading hierarchy, typography, spacing, safe areas, control affordances, themes, accessibility, keyboard/focus, empty/loading/error states and honest copy. Keep Future Self's voice and identity. Distinguish correctness defects, taste choices and optional ornament. Any substantial copy/value/monetization change returns to a founder decision. Do not imitate competitor assets.

Record selected demo IDs/recordings, rationale, layout/typography direction, reusable motion parameters, interruption and accessibility behavior, haptic policy and acceptance examples in DECISIONS. Technical defects go to WORK; approved design implementation becomes scoped batches. Remove/isolate prototypes from release scope later, not by deleting unexplained user files now.

## Stop
Request G3: chosen native motion/UI direction and the resulting implementation batches (including approved technical batches from G2). Once approved, mark Session3 complete, next_stage=4 and checkpoint; continue under the saved policy only within the approved implementation envelope. Founder approval of the demonstrated direction must precede its production-code rollout.
